import pdb

rango = 0

def iterate(rango):
    pdb.set_trace()
    new_list = []
    pdb.set_trace()
    for item in rango:
        pdb.set_trace()
        new_list.append(item)

if __name__ == '__main__':
    iterate(range(1,15))